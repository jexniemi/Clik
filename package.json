{
  "name": "newsexpress",
  "version": "1.0.0",
  "description": "",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "cd ui && npm install && npm test",
    "travis": "cd ui && npm test",
    "heroku-postbuild": "cd ui && npm install && npm install --only=dev --no-shrinkwrap && npm run build"
  },
  "engines": {
    "node": "8.7.0"
  },
  "author": "jexniemi",
  "license": "ISC",
  "dependencies": {
    "express": "^4.16.2"
  }
}
